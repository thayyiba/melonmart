# Contributing

Use feature branches and conventional commits (`feat:`, `fix:`, `docs:`, `test:`, `refactor:`). Keep database changes in numbered migration files. Never commit API keys, passwords, H2 data files, or `target/`. Run `mvn clean verify` before merging.
